import { SortProgressPipe } from './sort-progress.pipe';

describe('SortProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new SortProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
